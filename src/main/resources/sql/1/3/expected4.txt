SELECT
    author,
    ROUND(SUM(price * amount), 2) AS Вартість,
    ROUND(SUM(price * amount) * 18 / 118, 2) AS ПДВ,
    ROUND(SUM(price * amount) - SUM(price * amount) * 18 / 118, 2) AS Вартість_без_ПДВ
FROM book
GROUP BY author;
