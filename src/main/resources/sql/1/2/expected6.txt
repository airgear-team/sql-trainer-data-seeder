SELECT author, title,
       ROUND(
         IF(author = 'Коцюбинський М.М.', price * 1.10,
           IF(author = 'Нечуй-Левицький І.С.', price * 1.05, price)
         ), 2
       ) AS new_price
FROM books;
